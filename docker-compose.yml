version: "3.9"

services:
  vantage:
    image: ${VANTAGE_IMAGE:-ghcr.io/aashishvinu/vantage:latest}
    pull_policy: always
    container_name: vantage
    ports:
      - "127.0.0.1:8000:80"
    restart: unless-stopped
